# @cineflow/workflow-engine (planned — Phase 10)

Will contain the versioned workflow JSON schema + Zod validator (brief §40),
node/edge execution engine, and cycle/capability validation. Workflow execution
never runs arbitrary AI-generated shell commands (§39, §54).
