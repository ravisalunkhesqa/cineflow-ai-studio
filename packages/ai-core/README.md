# @cineflow/ai-core (planned — Phase 4)

Will contain: `ModelRegistry`, `ModelRouter`, `ContextAssembler`, `PromptComposer`,
`XKiroProvider` (implements `AIProvider` from `@cineflow/provider-sdk`), and the
Flow Agent's typed tool system (read tools vs. write tools per brief §84).

Not implemented yet — this package is scaffolded so `apps/api` can depend on it
without a restructure later.
