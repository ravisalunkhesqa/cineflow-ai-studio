import { ComingSoon } from "@/components/shell/coming-soon";
export default function GlobalLocationsPage() {
  return (
    <ComingSoon
      title="Locations"
      phase="a later phase (cross-project library)"
      note="Locations are managed per-project today — open a project, then its Locations tab."
    />
  );
}
