import { ComingSoon } from "@/components/shell/coming-soon";
export default function GlobalCharactersPage() {
  return (
    <ComingSoon
      title="Characters"
      phase="a later phase (cross-project library)"
      note="Characters are managed per-project today — open a project, then its Characters tab."
    />
  );
}
